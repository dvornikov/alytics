{
  "name": "alytics",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^3.3.7",
    "column-resizer": "^1.0.2",
    "json-server": "^0.12.1",
    "npm-run-all": "^4.1.1",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-redux": "^5.0.6",
    "react-scripts": "1.0.16",
    "redux": "^3.7.2",
    "redux-thunk": "^2.2.0"
  },
  "proxy": "http://localhost:4000",
  "scripts": {
    "start:api": "json-server --port=4000 --watch ./db.json",
    "start:app": "react-scripts start",
    "start": "npm-run-all --parallel start:*",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
